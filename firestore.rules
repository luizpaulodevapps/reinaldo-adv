rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * LexFlow ERP - Blindagem de Acesso
     * Conforme diretriz: Usuário autenticado = Acesso Total.
     */
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}