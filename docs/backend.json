
{
  "entities": {
    "Client": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Client",
      "type": "object",
      "description": "Represents a client (either individual or corporate) of the legal firm, storing their complete registration information.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "documentNumber": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phone": { "type": "string" },
        "address": { "type": "string" },
        "city": { "type": "string" },
        "state": { "type": "string" },
        "zipCode": { "type": "string" },
        "type": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "name", "documentNumber", "type", "createdAt"]
    },
    "Notification": {
      "title": "Notification",
      "type": "object",
      "description": "System-wide notification for users.",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "title": { "type": "string" },
        "message": { "type": "string" },
        "type": { "type": "string", "enum": ["deadline", "hearing", "lead", "financial", "system"] },
        "severity": { "type": "string", "enum": ["info", "warning", "critical"] },
        "read": { "type": "boolean" },
        "link": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "Interview": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Interview",
      "type": "object",
      "description": "Captured details from a potential client intake or witness interview.",
      "properties": {
        "id": { "type": "string" },
        "clientId": { "type": "string" },
        "clientName": { "type": "string" },
        "type": { "type": "string", "enum": ["Inicial Trabalhista", "Cível", "Preparação de Testemunha"] },
        "status": { "type": "string", "enum": ["Em Rascunho", "Concluída", "Convertida em Processo"] },
        "interviewerId": { "type": "string" },
        "data": { "type": "object" },
        "aiSummary": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      }
    },
    "Activity": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Activity",
      "type": "object",
      "description": "A CRM activity related to a lead or client.",
      "properties": {
        "id": { "type": "string" },
        "leadId": { "type": "string" },
        "type": { "type": "string", "enum": ["Lembrete", "Reunião", "Ligação", "E-mail", "WhatsApp", "Nota"] },
        "responsibleId": { "type": "string" },
        "responsibleName": { "type": "string" },
        "subject": { "type": "string" },
        "scheduledAt": { "type": "string", "format": "date-time" },
        "duration": { "type": "number" },
        "description": { "type": "string" },
        "status": { "type": "string", "enum": ["Planejado", "Realizado", "Cancelado"] },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "StaffProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "StaffProfile",
      "type": "object",
      "description": "Represents a legal team member including DP and System Access info.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string" },
        "isActive": { "type": "boolean" },
        "paymentType": { "type": "string", "enum": ["Mensalista", "Por Demanda", "Parceria (Porcentagem)"] },
        "commissionPercentage": { "type": "number" },
        "baseSalary": { "type": "number" },
        "oabNumber": { "type": "string" },
        "phone": { "type": "string" },
        "cpf": { "type": "string" }
      }
    },
    "Process": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Process",
      "type": "object",
      "description": "Details of a legal process.",
      "properties": {
        "id": { "type": "string" },
        "clientId": { "type": "string" },
        "clientName": { "type": "string" },
        "processNumber": { "type": "string" },
        "caseType": { "type": "string" },
        "status": { "type": "string" },
        "description": { "type": "string" },
        "court": { "type": "string" },
        "vara": { "type": "string" },
        "defendantName": { "type": "string" },
        "responsibleStaffId": { "type": "string" },
        "responsibleStaffName": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "FinancialTitle": {
      "title": "FinancialTitle",
      "type": "object",
      "description": "Financial entry for the legal firm (income or expense).",
      "properties": {
        "id": { "type": "string" },
        "type": { "type": "string", "enum": ["Entrada (Receita)", "Saída (Despesa)"] },
        "category": { "type": "string" },
        "description": { "type": "string" },
        "value": { "type": "number" },
        "dueDate": { "type": "string", "format": "date" },
        "status": { "type": "string", "enum": ["Pendente", "Pago", "Recebido", "Liquidado", "Cancelado"] },
        "originBank": { "type": "string" },
        "destinationBank": { "type": "string" },
        "responsibleStaffId": { "type": "string" },
        "clientName": { "type": "string" },
        "processNumber": { "type": "string" }
      }
    }
  },
  "auth": {
    "providers": ["google"]
  },
  "firestore": {
    "/notifications/{notificationId}": { "schema": { "$ref": "#/backend/entities/Notification" }, "description": "User notifications." },
    "/staff_profiles/{staffId}": { "schema": { "$ref": "#/backend/entities/StaffProfile" }, "description": "Staff access and roles." },
    "/employees/{employeeId}": { "schema": { "$ref": "#/backend/entities/StaffProfile" }, "description": "Detailed DP records." },
    "/clients/{clientId}": { "schema": { "$ref": "#/backend/entities/Client" }, "description": "Client registration data." },
    "/processes/{processId}": { "schema": { "$ref": "#/backend/entities/Process" }, "description": "Legal process details." },
    "/interviews/{interviewId}": { "schema": { "$ref": "#/backend/entities/Interview" }, "description": "Client intake interviews." },
    "/diligences/{diligenceId}": { "schema": { "$ref": "#/backend/entities/Diligence" }, "description": "Internal staff tasks." },
    "/financial_titles/{titleId}": { "schema": { "$ref": "#/backend/entities/FinancialTitle" }, "description": "Financial entries." },
    "/activities/{activityId}": { "schema": { "$ref": "#/backend/entities/Activity" }, "description": "CRM activities." },
    "/courts/{courtId}": { "schema": { "$ref": "#/backend/entities/Court" }, "description": "Registry of courts." },
    "/freelancers/{freelancerId}": { "schema": { "$ref": "#/backend/entities/Freelancer" }, "description": "Registry of freelancers." },
    "/counterparties/{counterpartyId}": { "schema": { "$ref": "#/backend/entities/Counterparty" }, "description": "Entities requesting acts." },
    "/freelance_diligences/{diligenceId}": { "schema": { "$ref": "#/backend/entities/FreelanceDiligence" }, "description": "Freelance tasks." }
  }
}
