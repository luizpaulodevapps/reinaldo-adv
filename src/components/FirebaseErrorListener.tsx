'use client';

/**
 * FirebaseErrorListener - Desabilitado
 * Permissões não são mais controladas nos componentes.
 * Se o usuário está logado, ele tem acesso total ao sistema.
 */
export function FirebaseErrorListener() {
  // Componente desabilitado - não controla permissões
  return null;
}
